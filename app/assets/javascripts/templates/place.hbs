{{#if isEditing}}
  {{partial 'place/edit'}}
  <button {{action 'doneEditing'}}>Done</button>
{{else}}
  <button {{action 'edit'}}>Edit</button>
{{/if}}

<h1>{{name}}</h1>
<h3>{{address}}</h3>

Some related content (pictures, comments, ...)
